### 1、gitHub命令

```java
echo "# yangblog" >> README.md
git init						
git add README.md
git clone 地址   					  //远程库：初始化到（本地库）
git commit -m "first commit"
git branch -M master
git remote add origin 
git push -u origin master
```



